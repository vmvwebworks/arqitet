class AddCadFileToProjects < ActiveRecord::Migration[7.0]
  def change
    # ActiveStorage no requiere columnas adicionales, solo asegurarse de que esté instalado
    # Si no tienes ActiveStorage instalado, ejecuta: rails active_storage:install
    # Aquí solo es una migración de referencia
  end
end
